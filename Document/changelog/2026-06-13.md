# 2026-06-13 变更日志

## 文档

- 建立 `Document/README.md` 文档目录约定。
- 完成 [`requirements.md`](../requirements.md) v1.2：
  - 基础视频监控、分组、状态历史、算法契约。
  - 简单模型 + VLM 分层算法策略。
  - 算法启用时段、VLM 调用抑制、通知接口。
  - 报警生命周期、ROI 标定、隐私安全、配置导入导出。
  - 验收用例。
- 新增 [`architecture.md`](../architecture.md)：
  - 总体架构、数据流、模块边界、风险。
- 新增 [`api.md`](../api.md)：
  - 已实现和待实现 Tauri commands / events。
  - 通知 Webhook payload。
- 新增 [`deployment.md`](../deployment.md)：
  - 开发、打包、数据目录、升级和发布检查清单。
- 新增 ADR 记录：
  - Tauri 2 + Vite 桌面架构。
  - 本地 JSON 作为第一版存储。
  - 简单模型 + VLM 分层算法。
  - Webhook / HTTP POST 通知。
  - 报警生命周期状态机。
  - ROI 使用归一化坐标。
  - 独立算法调度器。
  - 画面事实、业务报警和运行状态分离。
- 补充设计优化：
  - 算法配置继承：系统默认 < 全局 < 分组 < 通道。
  - MVP / 生产目标两级算法验收指标。
  - 配置导入冲突策略。
  - `ChannelRuntimeStatus` 运行状态模型。
- 收敛项目结构：
  - `Video/` 改为根目录平铺测试视频。
  - 删除空壳目录和占位文档目录。
  - README 只描述真实存在的目录和能力。
  - 明确 `Tools/push_streamer` 当前是 CLI 占位，ffmpeg 推流为后续任务。
  - 需求文档新增 MVP 分阶段范围。

## 实现状态摘要

- 前端基础 UI 已完成：登录、实时监控、监控总览、视频管理、系统设置、系统日志。
- Tauri 后端基础 commands 已完成：鉴权、视频源、分组、状态历史、密码修改、数据目录。
- 前后端字段适配层已补齐：前端统一使用 `camelCase`，后端存储继续兼容现有 `snake_case`。
- 配置存储框架已补齐：算法配置、ROI 配置、通知配置、安全配置会自动创建默认 JSON 文件。
- 运行状态模型已补齐：`ChannelRuntimeStatus`、`get_channel_runtime_status`、`ecoalert://runtime_status`。
- 算法调度器骨架已补齐：
  - 新增 `pipeline/scheduler.rs`。
  - 支持全局 / 分组 / 通道配置继承的有效配置计算。
  - 支持启用时段、例外时段和跳过原因。
  - mock 算法 ticker 已接入 scheduler，并推送 `ecoalert://algorithm_schedule`。
- 报警状态机骨架已补齐：
  - 新增 `alarm_records.json`。
  - 支持 `list_alarms`、`ack_alarm`、`resolve_alarm`。
  - mock 算法状态变化会生成 / 恢复 `AlarmRecord`，并推送 `ecoalert://alarm`。
- 通知发送骨架已补齐：
  - 新增 `pipeline/notifier.rs` 和 `notification_history.json`。
  - 支持通知目标配置、事件过滤、冷却判断、模板变量替换、HTTP / `mock://` 测试发送。
  - 支持 `list_notification_history`、`test_notification_target`、`resend_notification`。
  - 报警触发 / 恢复会驱动通知发送，发送结果推送 `ecoalert://notification` 并写入历史。
- 系统设置 UI 已接入：
  - 全局算法配置：启停、启用星期、启用时间窗、简单模型周期、VLM 周期 / 上限、阈值、报警保持和恢复策略。
  - 通知配置：目标新增、测试、删除。
  - 通知历史：最近记录、失败重发。
- 监控总览 UI 已接入：
  - 报警记录列表。
  - 报警确认和手动恢复操作。
  - `ecoalert://alarm` 事件驱动总览刷新。
- 算法 pipeline 和 stream 模块为骨架，真实算法和 RTSP 转码待实现。
- ROI 标定、真实算法、通道级算法覆盖、隐私安全配置 UI 待实现。
